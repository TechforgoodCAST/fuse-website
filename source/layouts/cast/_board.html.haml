%section#board.bg-light-gray.ph4-ns.pa2
  = partial 'components/section_header', locals: { title: 'Board Members' }

  .tc.center.mw8.flex.flex-wrap.justify-center
    - data.people.board.select(&:cast).each do |person|
      .w-50.w-20-m.dib.pa3.hide-child.relative
        = partial 'components/linkedin_overlay', locals: { link: person.linkedin }
        = image_tag person.image, class: 'br3'
        %h4.f6.black= person.name
        %h6.fw4= person.description
