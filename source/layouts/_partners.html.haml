%section.pa2
  .tc.center
    = partial("components/section_header", :locals => { :title => "Clients and Supporters"})
    .flex.flex-wrap
      - data.partners.partners.select { |partner| partner.role == 'cast' }.each_with_index do |partner, i|
        .w-50.pa2
          %a= image_tag partner.image
