%section#team.tc.bg-navy.pv5
  .h3.f3.fw4.white The Team

  .cf.tc.center.mw8.flex.flex-wrap.justify-center
    - data.people.team.select(&:fuse).each do |person|
      .w-50.w-20-m.fl.dib.pa3.hide-child.relative
        = partial 'components/linkedin_overlay', locals: { link: person.linkedin }
        = image_tag person.image, class: 'br3'
        %h4.white= person.name
        %h5.white.fw4= person.fuse_role
